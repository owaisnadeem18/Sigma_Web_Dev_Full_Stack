"Use client"; // If I have a component in NEXT JS of client side , then still I can embed it with the server side component of my website in NEXT JS

import React from "react";

const Navbar = () => {
  return (
    <>
      <br />I am a Navbar
    </>
  );
};

export default Navbar;
