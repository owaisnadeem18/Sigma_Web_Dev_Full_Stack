// To write if statement syntax in one simple line is ternary operator

alert("Welcome");
age = prompt("Enter your age for elections 2024 : ");

document.write(
  age > 18 ? "You can cast Vote" : "You are not eligible to cast vote"
);
