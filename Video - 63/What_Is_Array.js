let arr = [7, 4, 3, 56, 90, 34, 90];

// To store diff kind of variables even having different data types (only in JS), in only one variable collectively is called as arrays

console.log(arr);

// arrays are mutable , you can make any kind of changes in your array and it always modifies your original array

arr[0] = 45;

console.log(arr[0]);
console.log(arr); // new array value modifies the array
